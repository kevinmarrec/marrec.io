{
  "apps": [
    {
      "name": "marrec.io",
      "script": "./node_modules/.bin/nuxt",
      "args": "start -c config",
      "instances": 2,
      "exec_mode": "cluster",
      "wait_ready": true,
      "listen_timeout": 3000
    }
  ]
}
